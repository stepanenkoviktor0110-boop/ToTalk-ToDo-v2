---
task: 3
wave: 1
depends: []
status: pending
---

# Task 3: Session state — add action tracking fields

## Description
Add new session state fields for tracking pending action choice.

## What to do

Update `getInitialSession()` in `src/bot.js`:

```js
{
  // ... existing fields ...
  awaitingAction: false,      // waiting for task/summary button press
  pendingAction: null,        // 'tasks' or 'summary'
  transcript: null,           // stored transcript for callback
}
```

Export `getInitialSession` (it's already exported).

## Verification
```bash
node -e "
import { getInitialSession } from './src/bot.js';
const s = getInitialSession();
console.log(s.awaitingAction === false ? 'OK' : 'FAIL');
console.log(s.pendingAction === null ? 'OK' : 'FAIL');
console.log(s.transcript === null ? 'OK' : 'FAIL');
"
```

## Files to modify
- `src/bot.js`
